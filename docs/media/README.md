# README media

`overview.svg` is an editable workflow diagram with no external image or font requests.

`badge-light.png`, `badge-dark.png` and `badge-unavailable.png` are screenshots of the compiled component. API responses use synthetic example.com fixtures: 0.18 g / grade A and unavailable data. No real website measurement is claimed. Images are captured at 2× pixel density.

`badge-compact-*.png` and `badge-minimal-*.png` use the same compiled component and synthetic fixture, showing both themes for the additional variants.
